script_NPI_testbed <- function( hostname='localhost', db_user='root', password='', dbName='testbed_results' ) {
  #' @export
#  NPI_testbed( hostname='mysql-server', db_user='root', password='secret', dbName='testbed_results' )
  NPI_parameters( hostname=hostname, db_user=db_user, password=password, dbName=dbName )
}

